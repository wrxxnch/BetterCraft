offhand_key (Key to switch items between hands) enum aux1 none,aux1,zoom,sneak,jump
offhand_wieldview (Show 3D model of held item) bool true
offhand_hotbar_size (Width in pixels per hotbar slot. Increase this if offhand overlay clips with hotbar.) int 64